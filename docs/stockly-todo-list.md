# Stockly 통합 To do list

마지막 정리: 2026-08-17

이 문서는 사용자 메모와 2026-08-13 프로젝트 점검 결과를 합친 Stockly 통합 작업 목록이다.

현재 로컬 작업 트리에는 `059_security_data_protection.sql`과 관련 앱·Edge Function 변경이 포함되어 있다. 완료 체크는 현재 소스 구현 상태를 뜻하며, 원격 migration·Edge Function 배포와 브라우저·실기기 검증은 `RELEASE-*`에서 별도로 확인한다.

## 사용 원칙

- 체크박스는 코드, 브라우저/기기, 필요한 DB 상태까지 확인한 뒤 완료 처리한다.
- 이미 구현 흔적이 있는 항목은 다시 만들기 전에 현재 동작부터 검증한다.
- 검증은 기본적으로 별도 매장인 `Stockly 테스트 매장`(store_id `<PRIMARY_TEST_STORE_ID>`)에서 진행한다.
- 테스트용 Supabase 계정은 다음 세 계정을 사용한다.
  - `stockly-test-admin@example.invalid`
  - `stockly-test-staff@example.invalid`
  - `stockly-test-staff-limited@example.invalid`
- `기본 매장`을 포함한 다른 매장에서 검증해야 할 경우에는 대상, 변경 내용, 복구 계획을 먼저 설명하고 사용자의 명시적 승인을 받은 뒤에만 진행한다.
- 승인받지 않은 운영 매장 데이터는 검증 대상으로 임의 변경하지 않는다.
- DB 변경은 새 migration으로 작성하고 적용 여부를 별도로 확인한다.
- 배포는 사용자가 명시적으로 지시했을 때만 진행한다.
- 큰 기능은 기획 확정, 작은 구현, 실제 사용 검증 순서로 진행한다.

### 테스트용 계정

- 기존 기본 검증 매장 `Stockly 테스트 매장`(`store_id`: `<PRIMARY_TEST_STORE_ID>`)
  - 관리자: `stockly-test-admin@example.invalid` (`user_id`: `<PRIMARY_TEST_ADMIN_USER_ID>`, `role`: `store_admin`)
  - 직원: `stockly-test-staff@example.invalid` (`user_id`: `<PRIMARY_TEST_STAFF_USER_ID>`, `role`: `staff`)
- 역할 검증용 테스트 매장 `Stockly 권한 테스트 매장`(`store_id`: `<ROLE_TEST_STORE_ID>`)
  - 관리자: `stockly-role-admin@example.invalid` (`user_id`: `<ROLE_TEST_ADMIN_USER_ID>`, `role`: `store_admin`)
  - 권한 직원: `stockly-role-staff@example.invalid` (`user_id`: `<ROLE_TEST_STAFF_USER_ID>`, `role`: `staff`)
  - 무권한 직원: `stockly-role-limited@example.invalid` (`user_id`: `<ROLE_TEST_LIMITED_USER_ID>`, `role`: `staff`)
- 테스트 계정은 위에 연결된 테스트 매장에서만 사용한다. 비밀번호는 저장소 문서에 평문으로 기록하지 않는다.

## P0. 보안과 데이터 보호

- [x] `SEC-001` 일반 직원의 계정 탈퇴 과정에서 다른 직원을 관리자로 승격할 수 없도록 수정
  - 일반 직원 탈퇴는 자기 계정 삭제만 수행한다.
  - 매장 관리자 탈퇴에만 관리자 이관을 요구한다.
  - Edge Function에서 요청자 역할과 이관 대상 역할을 다시 검증한다.
- [x] `DATA-001` 재고 수량 변경, 재고 로그 기록, 발주 상태 정리를 하나의 DB 트랜잭션으로 통합
- [x] `DATA-002` 두 명 이상이 동시에 재고 작업을 할 때 수량 덮어쓰기를 방지
- [x] `DATA-003` 발주 확정 목록 교체, 개별 추가·삭제, 전체 취소를 DB 트랜잭션으로 통합
- [x] `DATA-004` 입고여부만 확인 품목의 로그 기록과 발주 상태 해제를 DB 트랜잭션으로 통합
- [x] `DATA-005` 품목 단위 관리 권한과 데이터 범위 재설계
  - 매장별 단위로 분리하거나 master만 관리하는 공용 단위로 제한한다.
  - 단위 이름 변경과 연결 상품 변경을 하나의 작업으로 처리한다.
- [ ] `DATA-006` 데이터 백업 로직 구상 및 구현 — 오류가 발생해도 입력 및 저장 데이터가 사라지지 않는 백업·복구 기능
  - **세부계획 선행 필요**: 보호 대상, 백업·스냅샷 범위, 보관 기간, 암호화 저장 위치, 권한·승인, 복구·롤백 절차를 먼저 문서화하고 설계 승인 후 구현한다.
  - 자동 백업 범위, 보관 기간, 복구 권한을 먼저 정한다.
  - 운영 매장 단위 백업과 개별 작업 취소를 구분한다.
- [ ] `DATA-007` 재고, 재고 로그, 발주 확정 상태의 불일치를 찾는 관리자용 진단 기능 또는 쿼리 작성
  - 2026-08-17 구현·원격 적용: migration `063_store_consistency_diagnostics.sql`과 운영 콘솔의 매장 필터·재검사·CSV 내보내기를 추가했고, UNION 정렬 오류 보정 `065_fix_store_consistency_diagnostics_order.sql`까지 원격 Supabase에 적용했다. 관리자 콘솔 브라우저 검증이 남아 있다.
- [ ] `DATA-008` 네트워크 오류와 중복 탭으로 인한 중복 저장 방지
  - 2026-08-17 구현·원격 적용: `mutation_requests`와 요청 결과 보관 helper, 재고·입고·발주 확정·금일 입고 삭제용 `*_idempotent` RPC를 추가하고 해당 화면에서 작업별 UUID를 재사용하도록 연결했다. 테스트 매장에서 동일 요청 2회 클릭 후 수량·로그가 한 번만 반영되는 것을 확인했으며, 실제 네트워크 차단 재현은 브라우저 도구 제한으로 남아 있다.

## P1. 현재 업무 흐름 개선

### To do list와 재고 파악

- [ ] `TODO-001` To do list의 주목적을 "다음 근무일 아침에 해야 할 일"로 명확하게 정리
  - 오늘 할 일, 다음 근무일 할 일, 반복 업무의 관계를 정의한다.
  - 홈 화면에서 목적이 바로 이해되도록 문구와 배치를 정리한다.
- [ ] `TODO-002` To do에 실행 시간과 담당 역할 또는 담당자를 지정하는 기능 추가
  - 예: 오전 9시, 오픈 담당, 마감 담당, 특정 직원.
  - 시간순 정렬과 미완료 표시 규칙을 함께 정한다.
- [ ] `TODO-003` 오늘 To do도 직접 등록할 수 있는지 현재 동작 검증 및 보완
- [ ] `TODO-004` To do 알림 방식 설계
  - 앱 내부 알림, 푸시 알림, 배지 중 필요한 범위를 결정한다.
  - iOS, Android, PWA 권한 흐름을 함께 고려한다.
- [ ] `TODO-005` 미완료 To do가 다음날 또는 다음 근무일로 이월되는지 검증하고 필요하면 수정
  - 원래 날짜와 이월 횟수를 기록할지 결정한다.
  - 반복 루틴이 중복 생성되지 않도록 한다.
- [ ] `TODO-006` 오래된 품목의 재고 파악 To do 생성 기능을 현재 DB와 실제 화면에서 검증
- [ ] `TODO-007` 오래된 품목 재고 파악 To do에서 입고·조정 없이 체크박스만 완료한 경우 다음 점검 주기에 다시 나타나도록 수정
  - 단순 완료를 실제 재고 확인과 동일하게 취급하지 않는다.
  - 완료 시점부터 새 주기를 계산할지, 마지막 실제 재고 확인일을 기준으로 할지 결정한다.
- [ ] `TODO-008` 지상 재고 파악, 지하 재고 파악, 단체 재고 파악 흐름 설계
  - 위치별 작업 목록과 완료 기준을 정의한다.
  - 창고/매장 수량 모델과 중복되지 않도록 한다.
- [ ] `TODO-009` 단체 또는 여러 담당자가 함께 확인해야 하는 To do 알림 방식 설계
- [ ] `TODO-010` 오래된 재고 파악 기능에 품목별 재고 파악 기간을 설정하는 기능 추가
  - 매장 공통 `threshold_days`를 기본값으로 유지하되, 품목별 기간이 있으면 해당 값을 우선 적용한다.
  - 품목별 기간 변경·삭제 시 이미 생성되었거나 이번 주기만 숨겨진 To do의 재생성 기준을 함께 정의한다.

### 재고 작업과 상품 병합

- [ ] `INV-001` 상품 병합 후 유지된 상품의 `재고 작업` 화면으로 이동하도록 수정
  - 병합 대상이 비활성화된 경우에도 존재하지 않는 상품 화면이 남지 않도록 한다.
  - 등록 화면 병합과 상품 수정 화면 병합의 복귀 경로를 함께 검증한다.
- [ ] `INV-002` `재고 작업 → 상품 수정 → 저장 → 뒤로가기`가 `재고 작업`으로 돌아가는지 검증 및 수정
- [ ] `INV-003` 재고 작업의 `마지막 확인일자` 의미 수정
  - 프랩 또는 주문으로 자동 차감된 경우에는 실제 재고 확인일을 갱신하지 않는다.
  - 사용자가 수기로 입고·출고·이동·조정한 경우 어떤 작업이 확인일을 갱신하는지 정의한다.
- [ ] `INV-004` NFC 태그로 상품을 찾아 재고 작업 화면을 여는 기능 기획 및 구현
  - 지원 기기, Web NFC와 네이티브 NFC 차이, 태그 쓰기 권한을 조사한다.
  - NFC 값에는 내부 UUID 또는 안전한 앱 링크만 저장한다.
  - 바코드 스캔과 동일한 중복 이동 방지 규칙을 적용한다.
- [ ] `INV-005` 일반 상품의 유통기간 또는 유통기한 입력 기능 추가
- [ ] `INV-006` 유통기한 임박·경과 품목의 폐기 알림과 폐기 기록 기능 추가
  - 기존 프랩 배치 유통기한 기능과 일반 상품 유통기한을 구분한다.

### 부족재고와 발주

- [x] `LOW-001` 컨펌 완료 품목이 부족재고 화면에서 사라지는 현재 동작을 실제 DB와 화면에서 검증
  - `confirmed_order_pending`과 확정 이력이 일치하는지 확인한다.
  - 입고 완료 또는 컨펌 취소 후 다시 표시되는 조건도 확인한다.
  - 2026-08-17 `Stockly 테스트 매장 20260817`의 `VERIFY-004 테스트 품목 20260817`으로 기준 재고 0, 최소재고 1 상태를 확인했다.
  - 권한 직원이 컨펌한 뒤 `컨펌 완료한 품목 숨기기`를 켜면 부족재고 목록이 비고, `confirmed_order_pending=true`와 확정 행 1건이 함께 확인됐다.
  - `발주하기` 화면에서 컨펌을 취소하면 품목이 다시 표시되고 확정 목록이 비었다.
  - 재입고 2개 후 창고 수량 2, `confirmed_order_pending=false`, `fresh_order_selected=false`, 부족재고 조건 false가 되었고 화면에서도 사라졌다.
  - 실사 수량을 0개로 낮추자 최소재고 이하 조건으로 부족재고에 다시 표시됐다. 마지막 확정 행은 취소해 0건으로 정리했다.
- [ ] `LOW-002` 부족재고 화면에서 재고 부족 이유를 작은 팝업 또는 보조 설명으로 표시
  - 최소재고 이하, 상태 기반 발주 필요, 긴급, 수동 추가를 구분한다.
  - 팝업이 반복적으로 업무를 방해하지 않도록 표시 조건을 정한다.
- [ ] `LOW-003` 직원별 `컨펌하기` 권한 부여 기능의 현재 동작 검증
  - UI 숨김뿐 아니라 RLS 우회가 차단되는지 확인한다.
  - migration `047_staff_task_permissions.sql`의 실제 적용 여부를 확인한다.
- [ ] `LOW-004` 입고예정 품목에 예상 도착일을 저장하도록 확장
  - 기본은 다음날로 하되 며칠 뒤 도착도 선택할 수 있게 한다.
  - 지연 입고, 휴무일, 부분 입고 처리 기준을 정의한다.
- [ ] `LOW-005` 발주예측 기능 기획
  - 판매 데이터 연동 전에는 재고 작업 로그와 과거 발주 주기로 가능한 범위를 정한다.
  - 판매 데이터 연동 후 소비 속도, 리드타임, 안전재고를 반영한다.
- [ ] `LOW-006` 발주 주기 추적 로직 설계
- [ ] `LOW-007` 발주 금액 파악 기능 추가
  - 상품별 가격, 최종 확인 시점, 공급처를 저장한다.
  - 가격 크롤링은 이용약관, 로그인, 로봇 정책, 가격 정확도 검토 후 진행한다.

### 입고·초대·공유

- [ ] `SHARE-001` 초대코드 복사 시 앱 다운로드 주소와 초대코드를 함께 공유
- [ ] `SHARE-002` 앱 설치 후 초대코드가 자동 입력되는 딥링크 또는 deferred deep link 흐름 구현
  - 웹, iOS, Android에서 동일 초대 링크를 사용하도록 설계한다.
  - 이미 매장에 소속된 계정의 초대코드 사용 제한을 유지한다.
- [ ] `SHARE-003` 초대코드 카카오톡 공유 및 OS 기본 공유 기능 추가
- [ ] `SHARE-004` 바코드 아카이브 기능 설계
  - 개인 매장 데이터와 공용 추천 데이터를 분리한다.
  - 잘못된 바코드, 중복 상품, 제조사 변경을 수정할 운영 절차를 만든다.
- [ ] `SHARE-005` 매장 간 바코드, 추천 최소재고, 발주처 링크 공유 기능 기획
  - 사용자가 명시적으로 공유한 데이터만 공용화한다.
  - 검증 상태, 신고, 수정 이력, 개인정보·영업정보 범위를 정의한다.
  - 수익화 모델과 데이터 품질 책임을 함께 검토한다.

## P2. 단체주문과 프랩 계산

- [ ] `GROUP-001` 단체주문 계산에서 프랩 품목을 발주량에 어떻게 포함할지 계산 모델 확정
- [ ] `GROUP-002` 단체주문 수량을 기준으로 필요한 프랩 제조량 계산
- [ ] `GROUP-003` 필요한 프랩 제조량을 기준으로 원재료 필요량과 부족 발주량 계산
- [ ] `GROUP-004` 현재 만들어 둔 프랩 재고를 먼저 차감하고 추가 제조량만 제안
- [ ] `GROUP-005` 프랩 손실률, 손질 전·후 중량, 유통기한을 계산에 반영
- [ ] `GROUP-006` 발주량 계산 결과에 원재료, 보유 재고, 프랩 재고, 추가 발주량의 산출 근거 표시
- [ ] `GROUP-007` `단체주문 계산` 화면에서 좌우 스크롤은 막고 상하 스크롤만 동작하도록 실제 기기에서 검증 및 수정
- [ ] `GROUP-008` 단체주문 연락처 입력·저장·표시 기능의 현재 동작 검증
- [ ] `GROUP-009` 카카오톡으로 받은 주문 폼을 그대로 저장할 수 있는 입력 방식 설계
  - 우선 텍스트 붙여넣기와 미리보기·확인 방식으로 시작한다.
  - 자동 파싱 실패 시 원문을 보존하고 사용자가 수정할 수 있게 한다.
- [ ] `GROUP-010` 단체주문 일정과 준비 시점 알림 추가

## P2. 상품 단위와 재고 모델

- [ ] `UNIT-001` 부피, 무게, 개수 기반 단위 모델의 현재 구현 범위 검증
- [ ] `UNIT-002` 한 포장 안의 낱개 수량을 표현하는 `단위당 유닛 수` 개념 정리
  - 예: 잡곡식빵 1봉에 빵 N개, 햄 1줄에 N장.
- [ ] `UNIT-003` 단위당 유닛 수가 입고, 출고, 프랩, 단체주문 계산에 미치는 영향 정의
- [ ] `UNIT-004` 기존 `낱개`, 중량, 부피 단위와 새 포장 환산이 충돌하지 않도록 변환 규칙 작성

## P2. UI·접근성 개선

참고 원칙:

- [Toss Design System 컴포넌트 설계 사례](https://toss.tech/article/toss-design-system): 다양한 케이스를 패턴화하고, 심미성과 사용성을 컴포넌트 수준에서 함께 보장한다.
- [제품이 커지면 디자인 시스템 가이드는 어떻게 개선돼야 할까](https://toss.tech/article/toss-design-system-guide): 큰 구조에서 상세 상태로 읽히는 규칙과 최악의 케이스·접근성 체크리스트를 함께 관리한다.
- 상세 실행 지침: [Stockly 디자인 지침](./stockly-design-guidelines.md)
- 화면을 예쁘게 꾸미는 것보다 `오늘 해야 할 일 → 가장 중요한 행동 → 완료 피드백`이 자연스럽게 이어지는 것을 우선한다.

- [ ] `UI-001` 상단 로고를 화면 가운데 배치하고 뒤로가기 버튼을 왼쪽에 고정
  - 제목, 우측 액션 수가 달라도 로고의 시각적 중앙이 유지되도록 한다.
- [ ] `UI-002` 전체 화면의 정보 밀도와 여백 기준 재정리
  - 한 화면의 우선 정보 수를 줄이고 중요하지 않은 설명은 접거나 상세 화면으로 이동한다.
  - 여백은 늘리되 주요 업무에 필요한 스크롤이 과도하게 증가하지 않도록 한다.
- [ ] `UI-003` 화면 확대를 막는 `user-scalable=no` 제거 검토
- [ ] `UI-004` 모달에 ESC 닫기, 포커스 가두기, 닫은 뒤 포커스 복귀 적용
- [ ] `UI-005` 성공·오류 메시지에 `aria-live` 또는 `role="alert"` 적용
- [ ] `UI-006` 하단 메뉴에 `aria-current`와 색상 이외의 선택 표시 추가
- [ ] `UI-007` 삭제·탈퇴 확인을 브라우저 기본 confirm 대신 일관된 앱 모달로 교체
- [ ] `UI-008` 44px보다 작은 모바일 터치 버튼 점검 및 수정
- [ ] `UI-009` Supabase 원문 오류를 사용자가 이해할 수 있는 한국어 메시지로 변환
- [ ] `UI-010` 오프라인 상태에서 저장 가능 여부, 초안 보존, 재시도 방식을 명확하게 표시
- [ ] `UI-011` 긴 상단 메뉴를 업무 영역별로 그룹화하고 기능 발견성을 개선
- [ ] `UI-012` 홈 대시보드를 "오늘 가장 중요한 업무" 중심으로 재설계
  - 입고·To do·인수인계를 동일한 크기로 나열하기 전에 부족재고, 입고예정, 미완료 업무의 수와 우선순위를 먼저 보여준다.
  - 첫 화면의 주 행동을 `스캔 시작` 또는 당일 매장의 최우선 업무로 정하고, 보조 정보는 목록·상세 화면으로 내린다.
  - 오늘/내일 전환은 유지하되 날짜 선택보다 처리해야 할 업무가 먼저 이해되도록 문구와 배치를 정리한다.
- [ ] `UI-013` 메인 내비게이션을 핵심 업무와 관리 기능으로 분리
  - 하단 탭은 우선 `홈 / 재고 / 스캔 / 더보기` 구조를 검토한다.
  - `부족재고`는 홈·재고의 배지 또는 필터로도 접근할 수 있게 하고, `작업로그`는 재고 상세 또는 더보기의 관리 영역으로 이동하는 안을 비교한다.
  - 현재 route와 권한을 유지한 채 정보구조만 먼저 문서화하고, 이동 경로를 검증한 뒤 UI를 변경한다.
- [ ] `UI-014` Stockly 전용 디자인 토큰과 컴포넌트 변형 정의
  - 색상, 간격, 모서리, 그림자, 타이포그래피, 터치 영역을 토큰 표로 만든다.
  - `Button`, `Status`, `ListRow`, `Surface`, `Modal`, `EmptyState`의 기본·선택·비활성·오류·성공 상태를 정의한다.
  - 페이지별 Tailwind 조합을 바로 전면 교체하지 않고 홈·부족재고·재고 작업에서 반복 패턴부터 공통 컴포넌트로 옮긴다.
- [ ] `UI-015` 로딩·빈 상태·오류·성공·되돌리기 패턴 통합
  - `StatusMessage`, skeleton, inline error, empty state, toast, confirm dialog의 사용 조건과 문구를 정한다.
  - 삭제·입고 완료·재고 저장처럼 되돌릴 수 있는 작업은 확인창보다 완료 후 `되돌리기`를 우선 검토한다.
  - 서버 오류 원문, 권한 오류, 오프라인 오류를 사용자에게 서로 다른 다음 행동으로 안내한다.
- [ ] `UI-016` 업무별 인터랙션·모션 규칙 정의
  - 스캔 성공, 재고 저장, 부족재고 추가, 발주 확정, 삭제·복원에 필요한 피드백을 각각 정의한다.
  - 공통 축소 애니메이션만 늘리지 않고 상태 변화·완료 위치·되돌리기 노출 시점을 설계한다.
  - `prefers-reduced-motion` 환경에서는 정보 전달을 유지하면서 장식 모션만 줄인다.
- [ ] `UI-017` 화면 문구와 상태 라벨 규칙 통합
  - 한국어/영어가 섞인 메뉴명을 정리하고, 기능명보다 사용자가 얻는 결과가 먼저 보이는 문구를 사용한다.
  - `입고예정`, `입고완료`, `부족`, `긴급`, `추가`, `확정`의 의미와 색상·아이콘 조합을 공통표로 만든다.
  - 버튼은 가능하면 한 화면에서 하나의 주 행동만 강조하고, 보조 행동은 시각적 위계를 낮춘다.
- [ ] `UI-018` 모바일·데스크톱 적응형 레이아웃 기준 수립
  - 모바일 하단 탭, 넓은 화면의 내비게이션·콘텐츠 폭·표/카드 전환 기준을 문서화한다.
  - 320px 폭, 큰 글씨, 키보드 표시, 긴 상품명·긴 버튼 문구에서 겹침과 잘림을 확인한다.
  - 화면을 단순히 축소하지 않고 기기 폭에 따라 정보량과 탐색 방식을 조정한다.

## P3. 오버뷰·플랫폼·성장 기능

- [x] `OVERVIEW-001` 재고를 한 화면에서 확인하는 오버뷰 기능의 현재 구현 범위 검증
  - 총 품목, 부족, 주의, 충분, 미확인, 위치별 수량을 어떤 순서로 보여줄지 확정한다.
  - 모바일 카드와 데스크톱 요약 화면의 정보량을 다르게 구성한다.
  - 2026-08-17 `Stockly 테스트 매장 20260817`에서 목록·오버뷰 전환, 자동 부족·주의·넉넉, 상태 기반 주의, 입고 확인, 중요 품목 필터, 창고·매장 수량, compact 표시를 확인했다.
  - 현재 화면은 상태별 품목 카드 중심이며 `전체/부족/주의/충분/미확인` 합계 카드는 표시하지 않는다. 합계 카드 정의·구현은 후속 범위로 남긴다.
- [ ] `PLATFORM-001` "코덱스에 포니테일 사용하기"의 의미와 적용 대상을 확인한 뒤 작업 범위 확정
  - 라이브러리, 디자인 도구, 에이전트 워크플로 중 무엇을 뜻하는지 확인이 필요하다.
- [ ] `PLATFORM-002` 내부 화면 딥링크, 새로고침 복원, 북마크 지원 방안 결정
- [ ] `PLATFORM-003` NFC, 푸시 알림, 딥링크의 iOS·Android·PWA 지원 범위 문서화

## P3. 성능과 유지보수

- [ ] `QUALITY-001` 페이지별 lazy loading과 코드 분할 적용
- [ ] `QUALITY-002` 초기 JavaScript 번들 크기 500KB 이하 목표로 개선
- [ ] `QUALITY-003` `App.tsx`의 인증, 라우팅, 네이티브 이벤트 로직 분리
- [ ] `QUALITY-004` `HomePage`, `LowStockPage`, `GroupOrderCalculatorPage`, `InventoryOperationPage`를 기능별로 분리
- [ ] `QUALITY-005` 반복되는 DB 쿼리를 도메인 서비스로 이동
- [ ] `QUALITY-006` `select("*")` 쿼리를 필요한 필드 조회로 점진적으로 변경
- [ ] `QUALITY-007` 앱과 관리자 콘솔에 Error Boundary 추가
- [ ] `QUALITY-008` 전역 lint에서 `dist-admin/`과 `tmp/`를 제외하고 기본 lint 명령 정상화
- [ ] `QUALITY-009` 높은 등급으로 보고된 간접 의존성 보안 경고를 업데이트하고 재감사

## P3. 테스트 자동화

- [ ] `TEST-001` 입고, 출고, 이동, 조정, 메모, 복원 테스트 추가
- [ ] `TEST-002` 두 직원의 동시 재고 작업 테스트 추가
- [ ] `TEST-003` 발주 추가, 컨펌, 확정 취소, 입고 완료 테스트 추가
- [ ] `TEST-004` 관리자, 직원, master 권한 및 우회 접근 테스트 추가
- [ ] `TEST-005` 계정 탈퇴와 관리자 이관 테스트 추가
- [ ] `TEST-006` 등록 상품, 보조 바코드, 미등록 상품 스캔 E2E 테스트 추가
- [ ] `TEST-007` To do 생성, 완료, 삭제, 이월, 반복 주기 테스트 추가
- [ ] `TEST-008` 프랩과 단체주문의 중량·부피·낱개 변환 테스트 추가
- [ ] `TEST-009` 메인 앱과 관리자 콘솔 빌드·소스 lint를 CI에서 실행

## 구현 흔적이 있어 먼저 검증할 항목

아래 항목은 현재 소스 또는 migration에 구현 흔적이 있다. 실제 운영 DB 적용과 브라우저·기기 동작을 확인한 뒤 완료 또는 수정으로 분류한다.

- [x] `VERIFY-001` 오래된 재고 파악 To do 생성과 삭제 후 다음 주기 재생성
  - 2026-08-16 테스트 매장에서 로그가 없는 `VERIFY-001 테스트 20260816` 품목으로 생성, 같은 날짜 새로고침 중복 방지, To do 클릭 후 재고 작업 이동, soft delete 후 같은 주기 숨김, 다음 주기 재생성을 확인했다.
  - 다음 주기 재생성은 실제 15일을 기다리지 않고 해당 테스트 To do의 날짜만 과거로 조정해 재현했다. 검증 후 두 To do 행은 soft delete했고 테스트 품목은 비활성화했으며, 창고·매장 수량은 0, 재고 로그는 0건으로 정리했다.
  - 최근 재고 로그 품목, 비활성 품목, 입고확인 전용 품목 제외 조건은 현재 코드의 `is_active`·`receipt_check_only` 필터와 테스트 매장 DB 상태를 함께 확인했다. 자동 프랩·단체주문 차감 로그를 실제 재고 확인으로 오인할 수 있는 `TODO-007`은 별도 보완 항목으로 남긴다.
- [x] `VERIFY-002` 오늘·내일 To do 등록, 수정, 삭제
  - 2026-08-16 테스트 매장에서 오늘·다음 근무일 To do 등록, 체크 상태 변경, 내일 To do 수정, 날짜별 표시를 확인했다.
  - 두 테스트 To do를 앱에서 삭제해 `deleted_at` 기록과 오늘·내일 화면 숨김을 확인한 뒤 정리했다.
- [x] `VERIFY-003` 일간·주간·월간·간격 반복 루틴
  - 2026-08-16 테스트 매장에서 일간·주간·월간·1일 간격 루틴을 저장하고, 오늘은 4개 모두, 다음 근무일은 일간·간격만 생성되는 것을 확인했다.
  - 새로고침 후 중복이 없었으며, 자동 생성 To do를 먼저 제거한 뒤 루틴을 정리하고 오늘·내일 화면과 DB에서 잔여 활성 항목이 없음을 확인했다.
- [x] `VERIFY-004` 컨펌 권한을 직원에게 부여하고 RLS로 보호하는 흐름
  - 2026-08-17 `Stockly 테스트 매장 20260817`에서 관리자, 권한 직원, 무권한 직원과 `VERIFY-004 테스트 품목 20260817`으로 검증했다.
  - 권한 직원은 `부족 재고` 화면에서 `컨펌` 버튼이 노출되고 실제 컨펌 저장이 성공했으며, `confirmed_order_items.confirmed_by`가 권한 직원 ID와 일치했다.
  - 무권한 직원은 브라우저에서 권한 부족 안내가 표시되고 `컨펌` 버튼이 노출되지 않았다.
  - `can_manage_store_task`는 관리자·권한 직원만 true였고 무권한 직원·다른 매장 대상은 false였다. 직접 INSERT는 권한 직원 201, 무권한 직원 RLS 403, `replace_confirmed_order_items`는 권한 직원 200·무권한 직원과 다른 매장 대상 400으로 차단됐다.
  - 검증 중 생성된 확정 행은 취소해 0건으로 정리했고, 테스트 품목은 활성·재고 0·`fresh_order_selected=true`·`order_completed=false`·`confirmed_order_pending=false` 상태로 남겼다.
- [x] `VERIFY-005` 컨펌 완료 품목을 부족재고에서 숨기는 흐름
  - `LOW-001`과 같은 테스트 품목으로 컨펌 후 숨김, 컨펌 취소 복귀, 입고 후 숨김, 최소재고 이하 재표시를 화면과 DB에서 확인했다.
- [x] `VERIFY-006` 재고 오버뷰 설정과 화면 표시
  - 2026-08-17 테스트 매장에서 migration `054`의 `abundant_multiplier` 기본값(1.5)과 설정값 반영을 확인했다. 기준 재고 1.8 품목이 설정 1.5에서는 `넉넉`, 2.0에서는 `주의`로 바뀌었고, 설정 행·임시 품목은 검증 후 삭제했다.
  - `부족`, 자동·상태 기반 `주의`, `넉넉`, `입고 확인` 카드와 중요 품목만 표시, 목록↔오버뷰 전환, 상세 카드의 창고·매장 수량, compact 접기/펼치기를 브라우저에서 확인했다.
  - 390×844 모바일 뷰에서 카드 2열과 상세 수량 표시를 확인했다. 직원 계정에는 관리자 전용 오버뷰 기준 편집 UI가 노출되지 않으며, migration RLS와 소스 조건도 관리자 쓰기로 제한된다.
- [ ] `VERIFY-007` 단체주문 연락처 저장과 표시
- [ ] `VERIFY-008` 중량, 부피, `개` 단위와 손질 후 중량 계산
- [ ] `VERIFY-009` 프랩 품목 유통기한·폐기 To do
- [ ] `VERIFY-010` 상품 수정 저장·취소 후 재고 작업으로 돌아가는 뒤로가기 흐름
- [ ] `VERIFY-011` 단체주문 화면의 모바일 좌우 스크롤 잠금과 상하 스크롤
- [ ] `VERIFY-012` `/?inviteCode=CODE` 링크의 URL 초대코드 자동 보관·입력
  - 코드에는 `inviteCode`, `invite_code`, `code` 파라미터와 localStorage 보관 구현이 있다. 로그인 전·후 브라우저와 native 링크 동작을 실제 확인한다.

## 세부 구현 계획

아래 계획은 2026-08-13 현재 소스 구조를 기준으로 작성했다. 파일명은 주 수정 지점이며, 실제 구현 전에는 관련 migration의 원격 적용 여부와 작업 트리의 기존 변경을 다시 확인한다.

### 구현 상태 판정 기준

- **신규 구현**: 현재 스키마나 화면에 필요한 데이터 구조 또는 흐름이 없다.
- **보완 구현**: 기본 흐름은 있지만 원자성, 권한, 복귀 경로, 접근성 같은 일부 조건이 빠져 있다.
- **검증 우선**: 코드와 migration이 이미 있으므로 운영 DB와 실제 화면에서 재현한 뒤 최소 수정한다.
- `SEC-001`은 2026-08-13 운영 Edge Function·웹 배포와 역할별 검증까지 완료했다. 현재 작업 트리의 `059_security_data_protection.sql`과 연관 앱 코드는 `DATA-001`~`DATA-005`의 소스 구현으로 반영되어 있으며, 이 항목들의 SQL 정적 검사, 새 로컬 DB 전체 migration 적용, 타입 검사, 동시성·권한 테스트 결과는 별도로 기록한다.

### 1. P0 보안·데이터 보호

- `SEC-001` — **운영 배포 및 역할별 검증 완료 (2026-08-13)**
  1. `AccountDeletionSection.tsx`에서 `staff`, 개인 매장 관리자, 공동 매장 관리자의 화면을 분리하고 직원에게는 이관 대상 선택 UI를 표시하지 않는다.
  2. `manage-account-deletion` Edge Function은 세션 사용자 ID로 프로필을 다시 조회한다. `staff` 요청은 본인 삭제만 허용하고, 공동 매장 `store_admin` 요청에만 같은 매장의 일반 직원을 이관 대상으로 요구한다.
  3. 클라이언트가 보낸 역할이나 매장 ID는 신뢰하지 않고 서버에서 계산한다. 다른 매장 사용자, 기존 관리자, master를 이관 대상으로 전달한 요청은 거부하고 같은 매장의 `staff`만 허용한다.
  4. 운영 Edge Function v3와 고객용 웹에 배포했다. 테스트 매장에서 직원 eligibility가 `staff`/구성원 0명으로 반환되고, 직원이 이관 대상을 강제로 전송한 요청은 HTTP 403으로 차단되며, 정상 직원 탈퇴는 본인 계정만 삭제하고 관리자 계정·매장을 유지하는 것을 확인했다. 단독 관리자는 기존 30일 복구 대기 흐름을 유지했다.
- `DATA-001`, `DATA-002` — **로컬 구현·migration·동시성 검증 완료, hosted 클라이언트 배포 전**
  1. `InventoryOperationPage.tsx`의 재고 갱신, 로그 삽입, 발주 플래그 해제를 `record_inventory_operation` RPC 한 번으로 이동한다.
  2. RPC에서 상품과 `inventory` 행을 `FOR UPDATE`로 잠그고, 화면이 읽은 `inventory.updated_at`을 `expected_inventory_updated_at`으로 받아 낙관적 잠금도 함께 검사한다.
  3. 수량 계산, 음수 방지, store 접근 권한, `receipt_check_only` 분기는 DB 안에서 재검증한다. 어느 단계든 실패하면 전체 트랜잭션을 롤백한다.
  4. 충돌 오류 시 입력 수량은 유지하고 최신 재고만 다시 불러온 뒤 사용자가 재시도하도록 한다. 두 세션이 같은 상품을 동시에 입고·출고·조정하는 테스트로 덮어쓰기 방지를 확인한다.
  5. 2026-08-15 정적 확인: 로컬·원격 migration 목록 001~059가 일치했고 linked DB lint가 통과했다. 로컬 빌드 산출물에는 `record_inventory_operation`과 `expected_inventory_updated_at`이 포함되어 있다.
  6. 2026-08-16 호스팅 검증: 임시 `DATA-001 테스트 검증 20260816` 품목으로 입고 3회, 이동 1회, 출고 1회, 창고 실사 5회를 실행해 각각 수량과 히스토리 로그가 반영되는 것을 확인했다. 이후 창고·매장 수량을 0으로 복원하고 품목을 비활성화했다. 두 세션을 같은 상태로 열어 첫 세션의 입고 1회 뒤 두 번째 세션의 stale 입고 2회를 저장했으나 충돌 오류 없이 7/1로 반영되었다. 호스팅 JavaScript 번들에는 `record_inventory_operation`과 `expected_inventory_updated_at` 문자열이 없어 DATA-001 이전 클라이언트가 배포된 상태로 판단한다. 호스팅 화면의 작업 선택기는 `실사·입고·이동·사용`, 히스토리는 내부값 `조정`을 표시하고 있어 로컬 소스의 표시 보완도 아직 배포되지 않았다.
  7. 2026-08-16 로컬 환경 확인: Docker Desktop은 macOS 관리자 암호가 필요한 설치 단계에서 중단되었고, Docker CLI와 Colima를 사용했다. 저장공간 확보 후 Colima VM과 DNS를 복구해 로컬 Supabase를 기동했고, 운영 DB·배포에는 접근하지 않았다.
  8. 2026-08-16 정적 확인: `npm run build`, `npx eslint src admin-console/src`, `git diff --check`가 통과했고, 로컬 bundle에 `record_inventory_operation`, `record_receipt_check`, `replace_confirmed_order_items`, `rename_product_unit`, 충돌 메시지와 `출고`·`실사` 표시가 포함되어 있다. 배포는 사용자 지시가 없어 진행하지 않는다.
  8-1. 전체 `npm run lint`는 생성물 `dist-admin/`과 기존 `tmp/`를 함께 검사해 2091개 오류로 실패했으며, 소스 범위 lint는 통과했다. 생성물 lint 정리는 별도 `QUALITY-008` 범위다.
  9. 2026-08-16 로컬 통합 확인: 빈 로컬 DB에 migration 001~059를 처음부터 적용하고 `db lint --local`이 통과했다. 검증용 매장·사용자로 입고·출고·이동·실사, 음수 방지, stale timestamp 충돌, 로그 삽입 강제 실패 rollback, 타 매장·입고 확인 품목 차단을 확인했으며 fixture와 로그를 정리했다. 기존 hosted 클라이언트는 DATA-001 이전 번들이므로 release 검증은 별도다.
- `DATA-003` — **로컬 구현·정합성·권한 검증 완료, hosted 클라이언트 배포 전**
  1. `LowStockPage.tsx`의 확정 목록 교체, 개별 추가, 개별 삭제, 전체 취소를 각각 RPC로 고정한다.
  2. 각 RPC는 `confirmed_order_items` 변경과 `products.order_completed`, `confirmed_order_pending`, `fresh_order_selected` 복구·정리를 한 트랜잭션에서 처리한다.
  3. `can_manage_store_task(store_id, 'order_confirmation')`을 RPC 내부에서 확인하고 다른 날짜·다른 매장 행을 수정하지 못하게 한다.
  4. 중간 실패 주입 테스트로 목록과 상품 플래그가 함께 롤백되는지, 취소 시 원래 수동 추가 상태가 복원되는지 확인한다.
  5. 2026-08-16 로컬 통합 확인: 확정 목록 교체·개별 추가·삭제·전체 취소, 권한 없는 직원·타 매장 관리자 차단, 다른 매장 상품·중복 상품 차단, 확정 목록 insert 실패 rollback을 확인했다.
- `DATA-004` — **로컬 구현·회귀 검증 완료, hosted 클라이언트 배포 전**
  1. `receiptCheck.ts`는 `record_receipt_check` RPC만 호출하게 하고 직접 `inventory`, `inventory_logs`, `products`를 순차 수정하지 않는다.
  2. RPC에서 대상 품목의 `receipt_check_only`와 store 접근 권한을 확인하고, 수량을 바꾸지 않은 입고 로그와 발주 플래그 해제를 같은 트랜잭션으로 처리한다.
  3. 로그 삽입 실패, 플래그 갱신 실패, 중복 탭 저장을 재현해 부분 저장이 남지 않는지 확인한다.
  4. 2026-08-16 로컬 통합 확인: 수량 불변 입고 로그, 발주 플래그 해제, 잘못된 품목 유형·타 매장 차단, 로그 삽입 실패 rollback을 확인했다.
- `DATA-005` — **로컬 구현·backfill·권한 검증 완료, hosted 클라이언트 배포 전**
  1. 권장안은 `product_units`를 `store_id` 기준 매장별 데이터로 전환하는 것이다. 기존 공용 단위는 모든 활성 매장에 복제하되 같은 매장 안에서만 이름을 unique로 둔다.
  2. `ProductUnitManagementPage.tsx`, `productUnits.ts`, 상품 등록·수정 화면의 단위 조회에 `currentStoreId`를 필수 전달한다. RLS는 같은 매장 조회, 관리자 쓰기만 허용한다.
  3. 단위 이름 변경은 `rename_product_unit` RPC에서 `product_units.name`과 같은 매장의 `products.unit_name`을 함께 갱신한다.
  4. migration 적용 전 행 수·중복 이름·연결 상품 수를 기록하고, 적용 후 매장별 단위 수와 상품 연결이 일치하는지 비교한다.
  5. 2026-08-16 로컬 통합 확인: 매장별 단위 조회 분리, 직원·타 매장 관리자 쓰기 차단, 이름 변경의 연결 상품 전파, 중복 단위 이름 충돌 rollback, `store_id` null·매장 내 중복 없음 확인을 완료했다.
- `DATA-006` — **세부계획 수립 후 신규 구현**
  0. 구현 전 세부계획에 보호 대상(초안·개별 작업·매장 전체), 대상 테이블·필드, 백업 주기·보관 기간, 암호화 저장 위치, 요청·복구 권한, 승인·감사 로그, 부분·전체 복구, 실패·롤백, 테스트 매장 검증 증적을 확정한다.
  1. 1단계는 사용자 입력 초안 보호다. 긴 폼은 `store_id + route + entity_id` 키로 IndexedDB에 자동 저장하고 서버 저장 성공 후에만 삭제한다.
  2. 2단계는 운영 복구다. 재고 변경은 기존 append-only 로그와 복원 RPC를 사용하고, 상품·설정·단체주문은 변경 전 JSON 스냅샷을 남기는 audit 테이블을 설계한다.
  3. 3단계 매장 백업은 관리자만 요청 가능한 Edge Function으로 JSON/CSV를 생성하고 암호화 저장, 보관 기간, 다운로드 감사 로그, 복구 승인 절차를 정한다.
  4. 전체 매장 복구와 개별 작업 취소를 같은 기능으로 만들지 않는다. 먼저 복구 시뮬레이션과 건수·합계 비교가 통과한 백업만 실제 복구 대상으로 인정한다.
- `DATA-007` — **로컬·원격 구현 완료, 브라우저 검증 대기**
  1. 읽기 전용 관리자 진단 RPC 또는 SQL view로 `inventory` 현재값과 마지막 유효 `inventory_logs.*_after`, `confirmed_order_items`와 `products.confirmed_order_pending`을 비교한다.
  2. 결과는 상품 ID, 불일치 종류, 기대값, 실제값, 마지막 변경 시각만 반환하고 자동 수정하지 않는다.
  3. 관리자 콘솔에 store 필터, CSV 내보내기, 재검사 버튼을 추가한다. 수정 RPC는 진단 결과를 검토한 뒤 별도 항목으로 승인받는다.
- `DATA-008` — **로컬·원격 구현 완료, 통합 검증 대기**
  1. 저장 시작 시 버튼 잠금만으로 끝내지 않고, 클라이언트가 작업마다 UUID `request_id`를 생성한다.
  2. 중요 RPC는 `(store_id, user_id, request_id, operation_type)` unique인 `mutation_requests`를 먼저 기록하고 같은 ID 재요청에는 최초 결과를 반환한다.
  3. 네트워크 타임아웃은 성공 여부 미확정 상태로 표시하고 같은 `request_id`로 재조회·재시도한다. 새 ID로 자동 재전송하지 않는다.
  4. 더블클릭, 브라우저 두 탭, 응답 직전 네트워크 차단 테스트로 로그와 수량이 한 번만 반영되는지 확인한다.

### 2. To do list와 재고 파악

- `TODO-001` — **기획 확정 후 UI 보완**
  1. 홈의 기본 의미를 `오늘 인계받은 일`과 `다음 근무일 아침 준비`로 정의한다. 오늘/다음 근무일 탭은 유지하고 루틴은 해당 날짜에 생성된 실행 항목으로만 보여준다.
  2. `HomePage.tsx`의 설명 문구와 추가 버튼을 선택 날짜 기준으로 통일하고, `TodoRoutinesPage.tsx`는 반복 규칙 관리 화면임을 명시한다.
  3. 수동 항목 → 루틴 → 오래된 재고 파악의 기존 표시 순서와 색상 의미는 유지한다.
  4. 직원이 첫 진입 시 오늘 할 일과 다음 근무일 할 일을 혼동하지 않고 각각 추가·완료할 수 있는지 사용성 확인한다.
- `TODO-002` — **신규 구현**
  1. `dashboard_todos`와 `todo_routines`에 `due_time`, `assignment_type(unassigned/role/user)`, `assignee_role`, `assignee_user_id`를 추가한다.
  2. 담당자는 같은 매장의 활성 프로필만 선택할 수 있게 하고 RLS/RPC에서도 재검증한다. 역할 담당은 `store_admin`, `staff` 또는 향후 정의할 업무 역할 값만 허용한다.
  3. `HomePage.tsx`는 시간 지정 항목을 시간순, 시간 없는 항목을 그 뒤에 정렬하고 담당 배지를 표시한다. 루틴 materialization 시 담당 정보를 복사한다.
  4. 담당자 탈퇴·역할 변경 시 항목을 삭제하지 말고 `미지정`으로 안전하게 전환한다.
- `TODO-003`, `VERIFY-002` — **검증 우선**
  1. 현재 `HomePage.tsx`의 오늘/다음 근무일 선택, 날짜 지정 추가, 수정, soft delete 쿼리를 테스트 매장에서 확인한다.
  2. 오늘 항목 추가 후 새로고침, 다른 계정 조회, 완료·미완료 토글, 삭제 후 재접속을 확인한다.
  3. 실패가 있으면 `task_date` 필터와 store scope만 좁게 수정하고 기존 다음 근무일 흐름은 바꾸지 않는다.
- `TODO-004` — **2단계 신규 구현**
  1. 1단계는 앱 내부 알림과 배지다. 미완료 항목 중 현재 시각이 `due_time`을 지났거나 오늘 예정인 건수를 홈·하단 메뉴에 표시한다.
  2. 2단계 푸시는 사용자별 알림 설정, 기기 토큰, 권한 상태, 발송 이력 테이블을 만든 뒤 Supabase scheduled function에서 발송한다.
  3. Capacitor iOS/Android와 PWA service worker의 권한 요청은 사용자가 알림을 켤 때만 실행한다. 거부 상태와 OS 설정 이동 안내를 분리한다.
  4. 중복 기기, 로그아웃, 담당자 변경, 루틴 삭제 시 예약 알림 취소 기준을 검증한다.
- `TODO-005` — **검증 후 신규 보완**
  1. 먼저 날짜가 지난 미완료 행이 현재 쿼리에서 사라지는지 확인한다. 권장 동작은 원본 행을 보존하고 다음 근무일에 `carried_from_todo_id`, `carryover_count`를 가진 새 실행 행을 한 번만 생성하는 것이다.
  2. 이월 RPC는 휴무일 설정으로 다음 근무일을 계산하고 `(carried_from_todo_id, task_date)` unique로 중복 생성을 막는다.
  3. 반복 루틴은 해당 날짜의 새 루틴 행이 이미 있으면 이전 미완료 행을 별도 이월하지 않고 `전일 미완료` 표시만 연결한다.
  4. 금요일→월요일, 임시 휴무일, 루틴과 수동 항목, 여러 번 새로고침을 테스트한다.
- `TODO-006`, `VERIFY-001` — **검증 우선**
  1. migration `041`, `044`와 `inventory_check_todo_settings`, `stale_inventory_product_id`의 원격 적용 여부를 확인한다.
  2. 마지막 유효 재고 로그가 기준일보다 오래된 테스트 품목, 로그가 없는 테스트 품목, 비활성·입고확인 전용 품목을 각각 확인한다.
  3. 삭제 시 이번 주기만 숨고 `threshold_days`가 지난 뒤 다시 생성되는지 서울 날짜 기준으로 확인한다.
- `TODO-007` — **보완 구현**
  1. 오래된 재고 To do의 점검 기준은 `is_completed`가 아니라 실제 재고 확인 시각으로 고정한다. 자동 프랩·단체주문 차감 로그는 확인 근거에서 제외한다.
  2. 해당 행의 체크박스 완료는 업무 표시만 닫고 `inventory`의 확인 시각을 갱신하지 않는다. 재고 작업에서 `조정` 또는 별도 `수량 그대로 확인`을 저장한 경우에만 `verified_at`을 갱신하고 연결 To do를 완료한다.
  3. 감사가 필요하면 `dashboard_todos.completion_source`와 `completion_inventory_log_id`를 추가해 `checkbox`와 `inventory_verification`을 구분한다.
  4. 체크박스만 완료한 품목은 다음 점검 주기에 다시 생성되고 실제 확인한 품목은 새 `verified_at + threshold_days`까지 생성되지 않는지 테스트한다.
- `TODO-008` — **기획 후 신규 구현**
  1. 창고/매장 수량과 별개인 `inventory_check_sessions`와 `inventory_check_session_items`를 만들고 범위를 `매장(지상)`, `창고(지하)`, `전체`로 정의한다.
  2. 세션 시작 시 대상 상품과 기대 수량을 스냅샷으로 고정하고 담당자가 실사 수량을 입력한다. 완료 전에는 실제 `inventory`를 바꾸지 않는다.
  3. 세션 종료 시 차이 목록을 검토한 뒤 승인된 항목만 기존 재고 조정 RPC를 호출한다. 여러 담당자의 입력 충돌과 중복 완료를 막는다.
  4. 완료율, 미확인 품목, 차이 수량, 담당자별 진행을 표시하고 전체 승인 전 결과를 취소할 수 있게 한다.
- `TODO-009` — **TODO-002/004 이후 구현**
  1. 공동 업무는 다중 사용자 배열보다 `assignment_group_id`와 참여자/역할 테이블로 모델링한다.
  2. 알림은 그룹 전체에 보내되 한 명의 확인이 전체 완료인지, 전원 확인이 필요한지 `completion_policy(any/all)`로 구분한다.
  3. 알림 읽음과 업무 완료를 분리해 저장하고, 마지막 필수 참여자가 완료했을 때만 전체 상태를 완료로 바꾼다.
- `TODO-010` — **신규 구현**
  1. `inventory_check_todo_settings.threshold_days`는 매장 기본 기간으로 유지하고, `store_id + product_id`별 기간을 저장하는 별도 설정 테이블을 추가한다. 품목이 다른 매장에 연결되지 않도록 foreign key, unique 제약, RLS를 함께 설계한다.
  2. `TodoRoutinesPage.tsx`에 활성 품목 검색·선택과 품목별 기간 입력 UI를 추가하고, 기간은 1일 이상 정수 범위로 검증한다. 설정이 없는 품목은 매장 기본 기간을 사용한다.
  3. `HomePage.tsx`의 오래된 재고 파악 생성 로직이 품목별 유효 기간을 적용하도록 수정한다. `receipt_check_only`, 비활성 품목, store scope, 실제 재고 확인 로그 제외 규칙은 유지한다.
  4. 기간 변경·삭제, 체크박스만 완료한 경우, To do 삭제 후 다음 주기 재생성까지 테스트한다. 특히 서로 다른 기간을 가진 테스트 품목 2개가 각자의 기준일에 맞춰 생성되는지 확인한다.

### 3. 재고 작업·상품 병합·유통기한

- `INV-001` — **보완 구현**
  1. 등록 중 병합은 이미 반환되는 유지 상품 ID를 사용해 `operation`으로 `replace` 이동한다.
  2. 수정 화면 병합도 `merge_products`의 반환값을 유지 상품 ID로 통일하고, 원본이 유지되든 후보가 유지되든 같은 경로로 이동한다.
  3. `ProductEditPage.tsx`에서 유지 상품 ID가 없거나 비활성화된 경우에만 재고현황 루트로 안전하게 이동한다.
  4. 등록→병합, 수정→내 상품 유지, 수정→후보 유지의 세 경우에서 뒤로가기 스택과 화면 상품 ID를 검증한다.
- `INV-002`, `VERIFY-010` — **검증 우선**
  1. `operation → product-edit` 진입 시 기존 navigation stack을 유지하고 저장·취소는 `getExitRoute()`로 같은 operation 상품에 `replace` 복귀하는지 확인한다.
  2. 저장 후 브라우저 뒤로가기, 앱 뒤로가기, iOS/Android 하드웨어 뒤로가기를 각각 확인한다.
  3. `returnTo`가 프랩·단체주문인 경로는 draft 복원 동작을 유지한다.
- `INV-003` — **신규 의미 분리**
  1. `inventory.verified_at`, `verified_by`, `verification_log_id`를 추가하고 화면의 `마지막 확인일자`는 이 값만 표시한다.
  2. 권장 기준은 수량을 실제로 센 `조정`과 새 `수량 그대로 확인`만 확인일을 갱신하는 것이다. 입고·출고·이동은 이동 기록이며 확인일을 갱신하지 않는다.
  3. `record_prep_operation`, 단체주문 계산·저장, 자동 플래그 정리는 `verified_at`을 절대 변경하지 않는다.
  4. 기존 데이터는 확실한 조정 로그만 backfill하고 애매한 품목은 `미확인`으로 둔다.
- `INV-004` — **PoC 후 신규 구현**
  1. 먼저 지원 기기 행렬과 Capacitor NFC 플러그인 유지 상태를 확인하고 테스트용 태그 한 종류로 읽기 전용 PoC를 만든다.
  2. 태그 payload는 `https://<공식도메인>/p/<product-id>` 같은 안전한 앱 링크와 버전만 저장하고 재고·매장 정보는 넣지 않는다.
  3. `src/lib/nativeNfcScanner.ts`가 payload를 검증한 뒤 store 접근 가능한 활성 상품만 조회하고 기존 `operation` route로 전달한다.
  4. `ScanPage.tsx`의 중복 이동 방지 ref와 동일한 단일 완료 guard를 공유한다. iOS·Android 각각 화면 꺼짐, 연속 태그, 다른 매장 태그를 검증한 뒤 태그 쓰기 UI를 추가한다.
- `INV-005`, `INV-006` — **일반 상품용 신규 배치 모델**
  1. 상품에 날짜 하나를 넣지 말고 `product_expiry_batches(product_id, location, quantity_remaining, expires_on, source_log_id)`를 추가해 여러 입고분을 표현한다.
  2. 유통기한 관리 사용 여부와 기본 보관일은 `products` 설정으로 두고, 입고 시 날짜·수량을 선택 입력한다.
  3. 출고·폐기는 FIFO로 배치 수량을 차감하되 기존 재고 수량과 로그를 같은 RPC에서 갱신한다. 프랩의 `prep_batches`는 별도 유지한다.
  4. 임박 기준일 설정, 홈 폐기 To do, 폐기 전 확인, 폐기 로그와 복원 가능 범위를 설계하고 배치 합계와 inventory 수량 불일치 진단을 추가한다.

### 4. 부족재고·발주

- `LOW-001`, `VERIFY-005` — **검증 우선**
  1. migration `051`, `058` 적용 여부와 `confirmed_order_items` 최신 확정 시각 대비 `confirmed_order_pending` 값을 SQL로 비교한다.
  2. 컨펌 후 숨김, 컨펌 취소 후 복귀, 입고 후 플래그 해제, 최소재고 이하 지속 시 재표시를 테스트 품목으로 확인한다.
  3. 불일치는 먼저 진단 쿼리 결과를 보존하고 reconciliation migration 또는 관리자 RPC로만 수정한다.
- `LOW-002` — **UI 보완**
  1. `LowStockPage.tsx`에서 한 품목에 적용된 이유를 `minimum`, `status`, `manual`, `urgent` 배열로 계산한다.
  2. 카드에는 우선 이유 배지 하나를 표시하고 정보 버튼을 누르면 나머지 이유와 현재값/기준값을 작은 popover로 보여준다.
  3. 키보드·터치로 열고 바깥 클릭·ESC로 닫히게 하며, 품목마다 자동으로 팝업을 띄우지 않는다.
- `LOW-003`, `VERIFY-004` — **검증 우선**
  1. migration `047`의 원격 적용과 `staff_permissions` RLS, `has_staff_permission`, `can_manage_store_task`를 확인한다.
  2. 관리자·권한 있는 직원·권한 없는 직원으로 버튼 노출과 RPC 직접 호출을 각각 테스트한다.
  3. UI 조건과 App route 접근 제어가 달라도 DB가 최종 차단하는지 확인한다.
- `LOW-004` — **신규 구현**
  1. `confirmed_order_items`에 `expected_arrival_date`, `ordered_quantity`, `received_quantity`, `receipt_status`를 추가한다.
  2. 컨펌 기본 도착일은 단순 다음날이 아니라 `businessCalendar.ts`의 다음 영업일로 계산하고 사용자가 날짜를 바꿀 수 있게 한다.
  3. 홈 입고예정 쿼리는 확정일의 다음날 추론 대신 `expected_arrival_date`를 직접 사용한다. 부분 입고는 잔량을 유지하고 완료 시에만 닫는다.
  4. 지연은 날짜를 자동 변경하지 않고 `지연` 상태로 표시하며 관리자가 새 예상일을 지정하게 한다.
- `LOW-005`, `LOW-006` — **데이터 축적 후 단계 구현**
  1. 판매 데이터가 없는 1단계에서는 유효 출고·프랩 소비 로그로 일평균 소비량, 최근 발주 확정 간격, 평균 입고 리드타임을 읽기 전용으로 계산한다.
  2. `(product_id, calculated_on, avg_daily_usage, lead_time_days, order_interval_days, confidence)` 스냅샷을 저장하고 최소 표본 미달은 예측하지 않는다.
  3. 제안 수량은 `리드타임 소비 + 안전재고 - 가용재고 - 입고예정`으로 계산하되 자동 발주하지 않는다.
  4. 실제 발주·입고 결과와 오차를 표시한 뒤 판매 연동이 확보되면 모델 입력을 확장한다.
- `LOW-007` — **신규 구현**
  1. `product_supplier_prices`에 상품, 공급처, 단가, 통화, 포장 수량, 확인 시각, 입력자를 이력으로 저장한다.
  2. 확정 목록은 당시 유효 단가를 snapshot으로 복사해 과거 합계가 가격 변경으로 바뀌지 않게 한다.
  3. 가격이 오래됐거나 없는 항목을 별도 표시하고 총액에는 포함 여부를 명시한다.
  4. 크롤링은 공급처 약관과 인증 방식을 검토한 별도 단계로 두고, 1차는 관리자 수동 입력·CSV 가져오기로 구현한다.

### 5. 초대·공유·바코드 아카이브

- `SHARE-001`, `SHARE-003` — **작은 UI 보완**
  1. `StaffManagementPage.tsx`에서 `https://<공식도메인>/?inviteCode=<CODE>`와 앱 다운로드 안내를 포함한 공유 문구를 생성한다.
  2. 복사 버튼은 코드만이 아니라 전체 초대 문구를 복사하고, 별도 `코드만 복사`를 보조 액션으로 둔다.
  3. `navigator.share`가 있으면 OS 공유 시트를 사용하고 없으면 클립보드로 fallback한다. 카카오톡은 OS 공유 대상 우선으로 하고 별도 SDK 도입은 필요성이 확인될 때 진행한다.
- `SHARE-002`, `VERIFY-012` — **웹 흐름 검증 후 네이티브 확장**
  1. 현재 URL query → localStorage pending code → 로그인 후 자동 입력 흐름을 웹에서 먼저 검증한다.
  2. 같은 HTTPS 주소를 iOS Universal Link와 Android App Link로 등록하고 Capacitor `appUrlOpen`에서 초대 URL과 OAuth URL을 분기한다.
  3. 미설치 시 다운로드/웹 가입 페이지로 보내고 설치 후 코드를 복원해야 한다면 검증된 deferred deep-link 제공자 도입 여부를 별도 결정한다.
  4. 이미 매장 소속인 계정, 만료·사용 완료 코드, 로그아웃 후 재로그인을 테스트한다.
- `SHARE-004`, `SHARE-005` — **운영 정책 선행 신규 기능**
  1. 개인 `products/product_barcodes`와 공용 `barcode_catalog`를 분리한다. 공용 테이블에는 원본 제공 매장 ID를 직접 노출하지 않고 동의·출처·검증 상태·수정 이력을 둔다.
  2. 상품 등록 시 공용 데이터는 추천값으로만 보여주며 사용자가 확인해야 자기 매장 상품에 복사한다.
  3. 신고, 중복 병합, 제조사 변경, 관리자 검수 큐를 먼저 만든 뒤 최소재고·발주처 링크 공유를 opt-in 필드로 확장한다.
  4. 공급처 계약 가격, 전화번호, 내부 메모는 공용화 금지 목록으로 둔다. 공용 데이터 품질 지표가 확보되기 전 수익화 기능을 연결하지 않는다.

### 6. 단체주문·프랩 계산

- `GROUP-001`, `GROUP-002`, `GROUP-003`, `GROUP-004`, `GROUP-005`, `GROUP-006` — **계산 모델을 먼저 확정한 뒤 순차 구현**
  1. 레시피 재료가 일반 상품 또는 프랩 품목을 참조할 수 있도록 `group_order_recipe_ingredients`에 `ingredient_type(product/prep)`과 `prep_item_id`를 추가한다. 둘 중 하나만 존재하도록 constraint를 둔다.
  2. 계산 순서는 `메뉴 주문량 → 프랩 총 필요량 → 사용 가능한 프랩 재고 FIFO 차감 → 추가 제조량 → 원재료 총 필요량 → 일반 재고 차감 → 발주 제안량`으로 고정한다.
  3. 프랩 제조량은 배치 단위로 올림할지, 소수 제조를 허용할지 품목별 `production_batch_size`로 정한다. 손실률은 원재료별 또는 프랩 품목별 한 곳에만 저장해 중복 적용하지 않는다.
  4. 기존 `unit_weight`, `processed_unit_weight`, `quantity_unit` 변환 helper를 하나의 순수 계산 모듈로 옮기고 중량·부피·개수를 서로 교환하지 않는다.
  5. 결과 타입에 `required`, `available_product_stock`, `available_prep_stock`, `additional_prep`, `raw_material_gap`, `conversion_trace`를 포함해 화면에 산출식을 펼쳐 보여준다.
  6. 계산은 먼저 읽기 전용 제안으로 제공한다. 사용자가 확정할 때만 제조 To do 또는 발주품목을 만들고, 계산 실행만으로 재고를 차감하지 않는다.
  7. 프랩 재고 충분/부족, 유통기한 임박 배치, 손질 손실, 낱개 환산, 같은 원재료를 여러 프랩이 공유하는 fixture로 단위 테스트한다.
- `GROUP-007`, `VERIFY-011` — **실기기 검증 후 보완**
  1. 달력 pointer handler와 `touch-action`, body scroll lock이 적용되는 구간을 iPhone Safari/WebView와 Android WebView에서 확인한다.
  2. 페이지 루트는 `overflow-x-hidden`, 표·달력 내부는 필요한 너비를 줄이고 `100vw`와 고정폭 자식이 넘치지 않게 수정한다.
  3. 세로 스크롤, 날짜 범위 드래그, 모달 내부 스크롤, 키보드 표시 상태를 함께 회귀 테스트한다.
- `GROUP-008`, `VERIFY-007` — **검증 우선**
  1. migration `031` 적용 여부와 `group_order_events.customer_contact` 저장·수정·재조회·타임라인 표시를 확인한다.
  2. 전화번호 형식을 강제 변환하지 않고 원문을 보존하되 앞뒤 공백과 빈 문자열만 정규화한다.
- `GROUP-009` — **신규 입력 보조 기능**
  1. 주문 원문, 파서 버전, 파싱 결과 JSON, 사용자가 확정한 값을 별도로 저장해 원문을 잃지 않는다.
  2. 1차 파서는 줄 단위 규칙으로 날짜·시간·단체명·연락처·메뉴/수량 후보를 만들고, 저장 전 미리보기에서 각 필드를 수정하게 한다.
  3. 모르는 메뉴나 애매한 수량은 자동 생성하지 않고 경고로 남긴다. 확정 후에만 기존 `group_order_events/items` 저장 흐름을 호출한다.
- `GROUP-010` — **TODO-004 기반 구현**
  1. 행사 시각 기준 `발주 마감`, `프랩 시작`, `최종 확인` offset을 매장 설정으로 둔다.
  2. 일정 저장 시 알림/To do 예약을 upsert하고 일정 변경·삭제 시 같은 source ID로 갱신·취소한다.

### 7. 상품 단위와 재고 모델

- `UNIT-001`, `VERIFY-008` — **검증 우선**
  1. `ProductEditPage.tsx`, `GroupOrderCalculatorPage.tsx`, 프랩 저장 RPC에서 g/kg, ml/L, 개와 손질 후 단위 변환 fixture를 실행한다.
  2. DB numeric 정밀도, 화면 반올림, 저장 후 재조회 값이 같은지 확인한다.
- `UNIT-002`, `UNIT-003`, `UNIT-004` — **기존 필드 의미를 보존하며 정리**
  1. `단위당 유닛 수`는 포장 1단위 안의 사용 가능한 개수로 정의한다. 현재 `unit_weight + unit_weight_unit='개'` 사용 흔적은 즉시 삭제하지 않고 새 helper 뒤에 호환시킨다.
  2. 도메인 helper는 `toBaseAmount`, `fromBaseAmount`, `packagesNeeded` 세 함수로 통일하고 count/weight/volume 차원을 타입으로 분리한다.
  3. 입고·출고의 재고 기본 단위는 상품 포장 단위를 유지하고 프랩·레시피 계산에서만 base amount로 변환한다. 발주량은 항상 포장 수로 올림한다.
  4. 기존 `낱개` 표시는 저장 의미를 바꾸지 않고 UI 라벨만 명확히 한다. 데이터 migration이 필요하면 변환 전후 샘플과 역변환 결과를 먼저 검증한다.

### 8. UI·접근성

- `UI-001` — `App.tsx` 상단을 3열 grid(`왼쪽 뒤로가기`, `가운데 로고`, `오른쪽 메뉴/역할`)로 바꾸고 가운데 칸을 화면 기준 중앙에 고정한다. 긴 제목은 본문 `PageTitle`에만 두고 320px 폭과 우측 액션 유무를 확인한다.
- `UI-002` — 먼저 화면별 정보 우선순위와 spacing token을 문서화한 뒤 `HomePage`, `LowStockPage`, `GroupOrderCalculatorPage`부터 한 화면씩 수정한다. 기능 제거 없이 보조 설명은 disclosure로 접고 모바일/데스크톱 스크린샷을 전후 비교한다.
- `UI-003` — `index.html`의 `maximum-scale=1.0`, `user-scalable=no`를 제거하고 모든 input을 16px 이상 유지한다. 확대 200%, 키보드 표시, 바코드 스캔 복귀를 iOS에서 확인한다.
- `UI-004` — 공통 `Modal` 컴포넌트와 `useDialogFocus` 훅을 만들고 열기 버튼 ref, 첫 포커스, Tab 순환, ESC, 닫은 뒤 포커스 복귀를 제공한다. 기존 모달을 한 번에 전면 교체하지 말고 화면별로 이전한다.
- `UI-005` — `StatusMessage`에 오류 `role="alert"`, 성공·정보 `role="status"`와 적절한 `aria-live`를 넣되 로딩 중 반복 렌더가 계속 읽히지 않게 메시지 변경 때만 갱신한다.
- `UI-006` — `BottomNav.tsx` 활성 버튼에 `aria-current="page"`, 배경/상단선/아이콘 채움 중 하나의 비색상 표시를 추가한다.
- `UI-007` — 공통 `ConfirmDialog`를 만든 뒤 데이터 손실 위험이 큰 탈퇴·재고 삭제·컨펌 취소부터 `window.confirm`을 교체한다. 확인 문구와 destructive 버튼 라벨은 기존 한국어 의미를 유지한다.
- `UI-008` — 정적 검색으로 버튼·클릭 요소를 목록화하고 320×568 viewport에서 실제 hit target을 측정한다. 예외는 시각 크기가 작아도 부모 hit area를 44×44 이상 확보한다.
- `UI-009` — `src/services/errors.ts`에 error code/constraint/RPC message 기반 한국어 매퍼를 추가하고 화면은 원문 대신 정규화된 오류를 받는다. 상세 원문은 개발 로그에만 남기고 인증·개인정보 값을 포함하지 않는다.
- `UI-010` — `navigator.onLine`만 신뢰하지 않고 저장 요청 실패를 `오프라인/서버 오류/권한 오류`로 구분한다. 지원 폼에는 로컬 초안과 재시도 상태를 표시하고 재고 변경은 오프라인 큐잉하지 않는다.
- `UI-011` — `TopMenu.tsx`를 `재고 운영`, `프랩·단체주문`, `기준정보`, `직원·설정` 그룹 데이터로 렌더링하고 현재 권한으로 비어 있는 그룹은 숨긴다. 기존 route와 권한 조건은 그대로 사용한다.
- `UI-012` — `HomePage.tsx`의 현재 3개 동일 패널 구조를 업무 우선순위 구조로 재설계한다. 먼저 정보 구조와 와이어프레임을 확정하고, `부족재고`, `입고예정`, `미완료 To do`, `오늘의 주 행동`을 실제 테스트 매장 데이터로 비교한다. 기능 삭제 없이 보조 패널을 접거나 하위 목록으로 내릴 수 있어야 한다.
- `UI-013` — `NAV_ITEMS`, `BottomNav.tsx`, `TopMenu.tsx`의 route를 매일 쓰는 업무와 관리 기능으로 분류한다. `홈 / 재고 / 스캔 / 더보기` 안을 기준안으로 만들되, 기존 URL·history·권한·스캔 복귀 동작이 깨지지 않는지 먼저 확인한다.
- `UI-014` — 현재 `.touch-button`, `.field`, `.panel`, `.primary-button`, `.secondary-button`과 페이지별 반복 클래스를 목록화해 토큰/컴포넌트 표를 만든다. radius·spacing·type scale·상태 색상·shadow를 먼저 정의하고, 홈·부족재고·재고 작업에 공통 variant를 적용한다.
- `UI-015` — `StatusMessage`, `Skeleton`, 각 페이지의 빈 상태와 `window.confirm` 호출을 목록화한다. 공통 `EmptyState`, `Toast`, `ConfirmDialog`, `InlineError` 도입 순서를 정하고, 재고 삭제·입고 완료·발주 컨펌 취소처럼 데이터 영향이 큰 흐름부터 이전한다.
- `UI-016` — `PressableButton`의 공통 터치 효과와 화면별 저장·스캔·복원 피드백을 분리해 interaction spec으로 기록한다. 성공/실패/대기 상태가 시각 효과에 의존하지 않도록 `StatusMessage`와 `aria-live`를 함께 사용하고 reduced-motion 회귀를 포함한다.
- `UI-017` — 메뉴명·버튼명·상태 배지·오류 문구를 수집해 `UI copy glossary`를 만든다. `To do list`, `재고현황`, `부족재고`, `컨펌`, `확정`의 표기를 통일하고, 문구 변경은 기존 route·권한·DB 의미를 바꾸지 않는 범위에서 진행한다.
- `UI-018` — 320×568, 일반 모바일, 태블릿/데스크톱, 큰 글씨 환경의 화면 기준표를 만든다. 각 기준에서 하단 내비게이션, sticky header, 표, 카드, 바텀시트, 키보드 표시, 긴 상품명이 겹치지 않는지 캡처로 검증한다.

### 9. 오버뷰·플랫폼

- `OVERVIEW-001`, `VERIFY-006` — migration `054`, `InventoryListPage.tsx`의 목록/오버뷰 전환, 중요 품목, 상태 계산, compact 표시를 테스트 매장에서 확인한다. 현재 상태별 카드와 위치별 수량은 확인했지만 `전체/부족/주의/충분/미확인` 합계 카드는 아직 없어 후속 구현 범위로 기록한다.
- `PLATFORM-001` — `포니테일`이 라이브러리, 디자인 파일, 자동화 도구 중 무엇인지 사용자 확인 전 구현하지 않는다. 대상이 확정되면 현재 React/Vite/Capacitor 호환성, 라이선스, 번들 영향, 대체안 순으로 별도 계획을 작성한다.
- `PLATFORM-002` — route별 URL serializer/parser를 만들어 상품·재고작업 같은 식별자 route를 HTTPS 경로로 표현한다. 인증 전에는 목적 route를 session storage에 보존하고 로그인 후 store 권한을 확인한 뒤 복원한다. 알 수 없거나 권한 없는 URL은 안전한 홈으로 보낸다.
- `PLATFORM-003` — 기능별 지원표에 iOS/Android/PWA의 API, 권한 요청 시점, 미지원 fallback, 테스트 기기를 기록한다. 문서 검증 후 NFC·푸시·딥링크 구현 항목의 acceptance checklist로 사용한다.

### 10. 성능·유지보수

- `QUALITY-001`, `QUALITY-002` — `App.tsx`의 페이지 import를 `React.lazy`로 전환하고 업무 묶음별 chunk를 만든다. 빌드의 1.2MB 수준 메인 JS를 기준선으로 기록하고 초기 route에 필요한 코드만 preload한다. 목표 500KB는 gzip/brotli와 raw 크기를 구분해 보고한다.
- `QUALITY-003` — 동작 변경 없이 `useAuthSession`, `useNativeAppEvents`, `useNavigationStacks` 순으로 추출한다. 한 단계마다 route key, 스크롤 복원, OAuth, Android back 동작을 회귀 확인한다.
- `QUALITY-004` — 큰 페이지는 먼저 순수 계산/helper, 다음 data hook, 마지막 UI section 순으로 분리한다. DB 호출을 새 컴포넌트로 흩뜨리지 않고 페이지 또는 도메인 서비스에 둔다.
- `QUALITY-005`, `QUALITY-006` — 중복 횟수와 변경 위험이 큰 쿼리부터 `TodoService`, `InventoryService`, `OrderConfirmationService`, `GroupOrderService`로 이동한다. 서비스 메서드에서 명시 필드 projection을 정의하고 타입을 `Pick<>`으로 좁힌다.
- `QUALITY-007` — 메인 앱과 관리자 콘솔 root에 별도 Error Boundary를 두고 route 재시도, 홈 이동, 오류 식별자를 제공한다. 인증·권한 오류는 boundary가 아니라 기존 화면 오류로 처리한다.
- `QUALITY-008` — `eslint.config.js` ignore에 `dist-admin/**`, `tmp/**`를 추가하고 `lint:src`, `lint:admin`, `lint` 스크립트를 분리한다. 생성물 제외 후에도 `src`와 `admin-console/src`가 모두 검사되는지 확인한다.
- `QUALITY-009` — `npm audit --omit=dev`와 전체 audit를 분리해 직접/간접, runtime/dev, 수정 가능 여부를 기록한다. lockfile만 바꾸는 최소 업데이트부터 적용하고 Capacitor, Vite, PWA 빌드 회귀를 확인한다.

### 11. 테스트 자동화

- `TEST-001`~`TEST-008` — 현재 테스트 러너가 없으므로 먼저 Vitest와 React Testing Library를 추가하고 날짜·단위·재고 계산을 순수 함수 테스트로 시작한다. Supabase 통합 테스트는 로컬 DB seed와 테스트 전용 store/user를 사용하며 운영 프로젝트를 사용하지 않는다.
  - `TEST-001`: 각 재고 작업 전후 수량, 로그 before/after, 복원 연결 ID를 검증한다.
  - `TEST-002`: 두 세션의 동일 `updated_at` 요청 중 하나만 성공하고 다른 하나는 충돌하는지 검증한다.
  - `TEST-003`: 발주 확정 RPC별 목록과 product 플래그 원자성을 검증한다.
  - `TEST-004`: 역할×permission 매트릭스로 화면 접근과 RPC/RLS 우회를 검증한다.
  - `TEST-005`: 직원 본인 탈퇴, 관리자 이관, 위조 대상, 복구 기간을 Edge Function 테스트로 검증한다.
  - `TEST-006`: Playwright의 브라우저 fallback 스캔 mock과 route 결과를 검증하고 native scanner는 실제 기기 체크리스트로 분리한다.
  - `TEST-007`: 서울 날짜, 휴무일, 이월, soft delete, 일/주/월/간격 루틴 중복 방지를 검증한다.
  - `TEST-008`: g/kg, ml/L, 개, 포장 환산, 손질률, 프랩 재고 선차감을 table-driven fixture로 검증한다.
- `TEST-009` — GitHub Actions에서 `npm ci → npm run build → npm run build:admin → npm run lint:src → npm run lint:admin → unit test` 순으로 실행한다. Supabase 통합 테스트는 별도 job과 최소 권한 secret을 사용하고 PR에서는 배포하지 않는다.

### 12. 검증 묶음 실행 순서

- `VERIFY-001`~`VERIFY-003`: `supabase migration list --linked`로 `041`, `044`, `046`, `052`, `053`을 확인한 뒤 `Stockly 테스트 매장`에서 To do 생성·완료·삭제·재생성·반복을 날짜별로 기록한다. 다른 매장을 사용하려면 사전 승인을 기록한다.
- `VERIFY-004`, `VERIFY-005`: 관리자/권한 직원/무권한 직원 3계정과 테스트 품목으로 컨펌 권한·숨김·취소·입고 복귀를 확인한다.
- `VERIFY-006`~`VERIFY-009`: 오버뷰, 연락처, 단위 변환, 프랩 유통기한은 DB row와 화면 표시를 함께 캡처하고 계산값은 수기로 한 번 대조한다.
- `VERIFY-010`~`VERIFY-012`: 브라우저 history, iPhone/Android gesture, 초대 URL의 설치 전후 흐름을 표로 기록한다. 기기 검증을 하지 못한 항목은 코드 완료와 분리해 미검증으로 남긴다.

### 13. 작업 묶음과 완료 조건

1. **P0-A**: 완료된 `SEC-001`을 제외하고 `DATA-001`~`DATA-005`의 기존 초안을 검토·수정한다. SQL 정적 검사, 새 로컬 DB 전체 migration 적용, build, source lint, 권한/동시성 테스트가 모두 통과해야 한다.
2. **P0-B**: `DATA-007`, `DATA-008`을 먼저 구현하고 `DATA-006`의 운영 백업은 세부계획 작성과 별도 설계 승인을 먼저 완료한다.
3. **검증 스프린트**: `VERIFY-001`~`VERIFY-012`를 실행해 이미 완료된 항목과 실제 수정 항목을 분리한다.
4. **업무 흐름 스프린트**: `TODO-007`, `INV-001`~`INV-003`, `LOW-001`~`LOW-004`를 처리한다.
5. **계산 스프린트**: `UNIT-*` 규칙을 고정한 뒤 `GROUP-001`~`GROUP-006`을 구현한다.
6. **플랫폼·성장 스프린트**: 초대 딥링크를 먼저 완료하고 NFC, 알림, 공용 바코드, 예측 순으로 진행한다.
7. **UI·제품 경험 스프린트**: `UI-012`와 `UI-013`의 정보 구조를 먼저 확정하고, `UI-014`~`UI-017`의 공통 규칙을 만든 뒤 화면별 구현을 진행한다. `UI-003`~`UI-011`, `UI-018`은 공통 컴포넌트가 적용되는 화면부터 순차 검증한다.
8. 각 묶음은 변경 파일, migration 적용 상태, 자동 테스트, 브라우저/기기 검증, 롤백 방법을 기록한 뒤에만 체크박스를 완료 처리한다.

## 배포 준비와 배포

- [ ] `RELEASE-001` 현재 작업 범위의 build, source lint, 테스트 결과 확인
  - 문서 변경만이면 `git diff --check`를 실행하고 build/lint 생략 이유를 남긴다. 코드 변경이면 `npm run build`, 정리된 source lint, 관련 자동 테스트를 모두 기록한다.
- [ ] `RELEASE-002` 필요한 Supabase migration의 로컬·원격 이력 비교
  - `npx supabase migration list --linked` 결과와 로컬 파일을 비교하고, 새 로컬 DB에 전체 migration을 처음부터 적용해 순서·함수 signature·RLS 오류를 확인한다.
- [ ] `RELEASE-003` Edge Function 변경 시 함수 배포 여부와 인증 동작 확인
  - 로컬/테스트 프로젝트에서 유효 토큰, 만료 토큰, 무권한 계정을 확인한 뒤 배포 대상 함수명과 롤백 버전을 기록한다. 함수 배포는 사용자 승인 후에만 한다.
- [ ] `RELEASE-004` 테스트 매장과 테스트 품목으로 브라우저 검증
  - 이름에 `테스트`가 포함된 품목만 변경하고 시작·종료 시 상품, 재고, 로그, 확정 목록 건수를 비교한다.
- [ ] `RELEASE-005` iPhone과 Android 실제 기기에서 핵심 흐름 검증
  - Capacitor local bundle 버전을 먼저 확인하고 로그인, 스캔, 재고 저장, 뒤로가기, 딥링크, 오프라인 오류를 기기별로 기록한다.
- [ ] `RELEASE-006` 변경 내용, 롤백 방법, 운영 영향 기록
  - 코드 롤백, migration forward-fix, Edge Function 이전 버전 복귀를 구분하고 데이터 변환이 있으면 되돌릴 수 없는 범위를 명시한다.
- [ ] `RELEASE-007` 사용자의 명시적 배포 승인 확인
  - 배포 대상 branch/commit, Supabase project, Vercel project, iOS/Android build를 한 번에 나열해 승인 범위를 확정한다.
- [ ] `RELEASE-008` 승인 후 빠른 배포 진행 및 배포 결과 확인
  - DB → Edge Function → 웹 순서처럼 의존성에 맞춰 적용하고, 단계마다 smoke test가 실패하면 다음 단계로 진행하지 않는다. 네이티브 앱은 별도 build와 배포 상태로 보고한다.

## 권장 진행 순서

1. 완료된 `SEC-001`은 닫고, 다음으로 `DATA-001`부터 `DATA-005`까지 migration·권한·동시성 검증을 진행한다.
2. `VERIFY-*`를 실행해 이미 구현된 항목과 실제 미완료 항목을 분리한다.
3. `TODO-007`, `INV-001`부터 `INV-003`, `LOW-001`부터 `LOW-004`를 처리한다.
4. 단체주문·프랩 계산 모델을 확정한 뒤 `GROUP-*`를 구현한다.
5. NFC, 알림, 공용 바코드, 발주예측 같은 플랫폼·성장 기능을 진행한다.
6. UI 작업은 다음 순서로 나눈다: `UI-012`·`UI-013` 정보 구조 → `UI-014` 토큰/컴포넌트 → `UI-015` 상태 패턴 → 화면별 적용 → `UI-003`~`UI-011`·`UI-018` 접근성/반응형 검증.
7. 각 묶음이 끝날 때 테스트와 실제 기기 검증을 추가한다.
8. 배포는 `RELEASE-007` 승인 후에만 실행한다.
